Задание

В этом спринте вы продолжите развивать сервис для управления мероприятиями: добавите в него механизмы обработки ошибок, фильтрацию данных и тестовое покрытие. 
Вы реализуете глобальный обработчик исключений через middleware, расширите API возможностями поиска событий по различным критериям с помощью LINQ и напишете юнит-тесты для проверки корректности работы бизнес-логики.
В результате выполнения проектной работы вы:
реализуете централизованную обработку ошибок с единым форматом ответа;
добавите возможность фильтрации событий по названию и датам;
добавите пагинацию событий;
создадите набор юнит-тестов для проверки бизнес-логики сервиса;
научитесь тестировать успешные и неуспешные сценарии работы приложения;
повысите качество и надёжность кодовой базы проекта.
Требования и функциональность

Проект использует C#, .NET 8 и выше, ASP.NET Core Web API.
Код приложения хранится в том же публичном GitHub-репозитории.
Проект собирается и запускается стандартными средствами (dotnet build, dotnet run).
Тесты запускаются командой dotnet test и успешно проходят.
Swagger доступен и корректно отображает новые параметры фильтрации.
Глобальная обработка ошибок возвращает единообразные JSON-ответы для всех типов исключений.
Фильтрация событий реализована с использованием LINQ.
Пагинация реализована с использованием LINQ.
Юнит-тесты покрывают основную бизнес-логику сервиса EventService.
Тесты используют фреймворк xUnit.
Как работать над заданием

Этап 1. Подготовка к работе

Убедитесь, что проектная работа первого спринта успешно завершена и принята.
Переключитесь на ветку main вашего репозитория и выполните git pull для синхронизации.
Создайте новую ветку sprint-2 из ветки main и переключитесь на неё.
Все работы по этой проектной работе ведите в ветке sprint-2.
Этап 2. Реализация глобальной обработки ошибок

Создайте middleware для глобальной обработки исключений: 
middleware перехватывает все необработанные исключения;
возвращается единообразный JSON-ответ с информацией об ошибке;
формат ответа содержит: код статуса, сообщение об ошибке;
рекомендуем использовать Problem Details (RFC 7807) или собственный формат.
Зарегистрируйте middleware в pipeline приложения.
Обеспечьте корректные HTTP-статусы для разных типов ошибок: 
400 Bad Request для ошибок валидации;
404 Not Found для ситуаций, когда ресурс не найден;
500 Internal Server Error для непредвиденных ошибок.
Опционально: добавьте логирование ошибок (используйте встроенный ILogger).
Этап 3. Добавление фильтрации событий

Расширьте метод GET /events параметрами фильтрации: 
title (string, опциональный) — поиск по названию (регистронезависимый, частичное совпадение);
from(DateTime, опциональный) — события, которые начинаются не раньше указанной даты;
to(DateTime, опциональный) — события, которые заканчиваются не позже указанной даты.
Реализуйте фильтрацию с использованием LINQ в сервисе EventService: 
применять фильтры только если параметры переданы;
все фильтры должны работать совместно (логическое И).
Убедитесь, что Swagger корректно отображает новые query-параметры.
Проверьте работу фильтрации через Swagger или другой HTTP-клиент.
Этап 4. Добавление пагинации событий

Расширьте метод GET /events параметрами фильтрации:  
page (int, опциональный, c значением по умолчанию 1) — страница, которую необходимо вернуть;
pageSize (int, опциональный, со значением по умолчанию 10) — количество элементов на странице.
Добавьте DTO PaginatedResult для возврата результата пагинации (общее количество событий, массив самих событий, номер текущей страницы, количество элементов на текущей странице).
Реализуйте пагинацию с использованием LINQ в сервисе EventService: 
получить нужные записи с использованием методов Skip и Take;
вернуть данные в PaginatedResult.
Убедитесь, что Swagger корректно отображает новые query-параметры.
Проверьте работу пагинации через Swagger или другой HTTP-клиент.
Этап 5. Написание юнит-тестов

Создайте отдельный тестовый проект (например, EventService.Tests): используйте xUnit, добавьте ссылку на основной проект.
Напишите тесты для сервиса EventService, покрывающие:
Успешные сценарии:
создание события;
получение всех событий;
получение события по ID;
обновление существующего события;
удаление существующего события;
фильтрация по названию;
фильтрация по датам (startDate, endDate);
пагинация событий;
комбинированная фильтрация.
Неуспешные сценарии:
попытка получить событие с несуществующим ID;
попытка обновить событие с несуществующим ID;
создание события с некорректными данными (если валидация в сервисе);
обновление события с некорректными датами (EndAt раньше StartAt).
Убедитесь, что все тесты проходят успешно (dotnet test).
Этап 6. Проверка и оформление

Протестируйте работу API через Swagger: 
проверьте фильтрацию событий с различными комбинациями параметров;
убедитесь, что ошибки возвращаются в едином формате.
Обновите README.md: 
добавьте описание новых параметров фильтрации для GET /events;
добавьте инструкции по запуску тестов (dotnet test);
укажите формат ответа при ошибках.
Убедитесь, что: 
проект собирается и запускается без ошибок;
все тесты проходят успешно.
Сделайте финальный commit и push всех изменений в ветку sprint-2 на GitHub.
Этап 7. Сдача работы

Создайте pull request из ветки sprint-2 в ветку main вашего репозитория.
Скопируйте ссылку на созданный pull request.
В тренажёре в уроке с проектом вставьте ссылку на pull request в поле для ссылки и нажмите «Отправить».
Ревьюер проверит вашу работу по чек-листу. Он оставит комментарии в pull request, если потребуются правки.
После того, как правки по всем комментариям будут внесены, ревьюер одобрит pull request.
Выполните слияние изменений в ветку main.
Чек-лист проверки для ревью

Работа возвращается без проверки, если:

Нет ссылки на репозиторий или репозиторий приватный/недоступен.
Проект не собирается (dotnet build падает).
Проект не запускается (dotnet run падает).
Тесты не запускаются (dotnet test падает).
Нет обновлённого README с описанием изменений.
Критические требования (без этого работа не принимается):

Реализован глобальный middleware для обработки исключений.
Middleware возвращает единообразный формат ошибок в JSON.
Корректные HTTP-статусы для разных типов ошибок (400, 404, 500).
Добавлена фильтрация в GET /events по параметрам: title, from, to.
Фильтрация и пагинация реализованы с использованием LINQ.
Создан тестовый проект с юнит-тестами.
Тесты покрывают успешные сценарии CRUD-операций, неуспешные сценарии и фильтрацию событий.
Все тесты проходят успешно (dotnet test выполняется без ошибок).
Swagger корректно отображает параметры фильтрации.
README содержит описание новых возможностей и инструкции по запуску тестов.
Хорошие практики (необязательно для зачёта):

использование Problem Details (RFC 7807) для единого формата ошибок;
логирование ошибок с использованием ILogger;
покрытие тестами edge cases (граничные значения дат, пустые строки в фильтрах и т. д.);
использование assertion-библиотек (FluentAssertions) для более читаемых тестов.
Отлично (всё выполнено на высоком уровне):

Middleware аккуратно структурирован и легко расширяем.
Фильтрация работает эффективно и покрывает все комбинации параметров.
Высокое покрытие тестами (включая граничные случаи).
Тесты хорошо организованы, читаемы и следуют принципу AAA (Arrange-Act-Assert).
История коммитов отражает логические этапы работы.
Следование принципам чистого кода и SOLID.
README содержит примеры запросов с фильтрацией и примеры ответов с ошибками.